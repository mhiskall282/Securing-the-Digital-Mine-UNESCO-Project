services:
  # 1. Python Inference Backend Server (Private Service)
  - type: pserv
    name: api-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/api_service.py"
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: "8001"

  # 2. Laravel Livewire Multi-tenant Dashboard (Public Web Service)
  - type: web
    name: minesec-dashboard
    env: php
    plan: free
    region: oregon
    buildCommand: "cd dashboard && composer install --no-dev --no-scripts --no-interaction && npm install && npm run build"
    startCommand: "cd dashboard && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        generateValue: true
      - key: MODEL_SERVER_URL
        value: "http://api-service:8001/api/analyze"
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: minesec-db
          property: host
      - key: DB_PORT
        value: 5432
      - key: DB_DATABASE
        fromDatabase:
          name: minesec-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: minesec-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: minesec-db
          property: password

# 3. PostgreSQL Database Instance
databases:
  - name: minesec-db
    plan: free
    region: oregon
