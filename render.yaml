services:
  # 1. Python Inference Backend Server (Pro Plus Tier: 8GB RAM, 4 CPU Cores)
  - type: web
    name: api-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/api_service.py"
    plan: pro_plus
    region: oregon
    envVars:
      - key: PORT
        value: "8001"

  # 2. Laravel Livewire Multi-tenant Central Dashboard (Pro Plus Tier: 8GB RAM, 4 CPU Cores)
  - type: web
    name: minesec-dashboard
    env: docker
    dockerfilePath: ./dashboard/Dockerfile
    plan: pro_plus
    region: oregon
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        generateValue: true
      - key: MODEL_SERVER_URL
        value: "http://api-service:8001/api/analyze"
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: minesec-db
          property: host
      - key: DB_PORT
        value: 5432
      - key: DB_DATABASE
        fromDatabase:
          name: minesec-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: minesec-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: minesec-db
          property: password

# 3. PostgreSQL Database Instance (Pro Plus 8GB Tier)
databases:
  - name: minesec-db
    plan: pro_plus_8gb
    region: oregon
